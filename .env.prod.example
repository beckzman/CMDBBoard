# Production Environment Variables Example
# Copy this file to .env and fill in the values

# Database Configuration
POSTGRES_USER=cmdb_user
POSTGRES_PASSWORD=secure_password_please_change
POSTGRES_DB=cmdb_database

# Backend Configuration
SECRET_KEY=super_secure_secret_key_change_me_in_production
DATABASE_URL=postgresql://cmdb_user:secure_password_please_change@postgres:5432/cmdb_database
# Allowed origins for CORS (comma separated)
CORS_ORIGINS=http://your-domain.com,https://your-domain.com,http://localhost

# Frontend Configuration (Not strictly needed for env file in prod unless injecting at build time, 
# but good to keep track of where it's pointing if you customize further)
VITE_API_URL=http://your-domain.com/api
